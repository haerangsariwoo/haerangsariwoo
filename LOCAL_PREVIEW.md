# 해랑사리우 모바일 리디자인 · 로컬 실행

> 최신 상태: 실제 Supabase API 연결을 확인했고 로컬 관리자 우회는 꺼져 있습니다. 아래는 초기 디자인 미리보기 작업 기록이며, 현재 연결·검증·릴리스 상태는 [MAIN_READINESS.md](MAIN_READINESS.md)를 참고하세요. 회원 데이터와 환경파일은 커밋하지 않습니다.

## 실행할 작업본
원래 Documents 작업 폴더의 일부 파일이 `dataless` 상태로 읽히지 않았고 Git pack/HEAD 조회도 실패했습니다. 원본 Git이나 읽을 수 없는 파일은 복구 명목으로 덮어쓰지 않았습니다.

동일한 원격 커밋 `3392ee113b77e63ce941b0db3391f78cedbfa112`에서 만든 정상 작업본에 변경을 모았습니다. 최종 코드와 실행 위치는 다음과 같습니다.

```sh
cd /Users/honggeunkim/Projects/haerangsariwoo-preview-20260913/apps/main
npm run dev -- --hostname 127.0.0.1
```

- 회원 홈: http://localhost:3010/home
- 새 로그인 화면: http://localhost:3010/login
- 관리자: http://localhost:3010/admin
- 의존성을 다시 설치할 때: `npm ci`
- 검사: `npm run lint`, `npm run build`
- Git 커밋·푸시·배포는 하지 않았습니다. `apps/recruit`는 수정하지 않았습니다.

## 화면 미리보기와 실제 기능 테스트의 차이
현재 `.env.local`은 화면 테스트용입니다. 개발 모드 + `LOCAL_ADMIN_BYPASS=1`일 때만 로컬 관리자 프로필로 입장합니다. 시작 주소 / 는 /home으로 이동하며 /login은 별도로 볼 수 있습니다.

Supabase 주소와 키가 로컬 자리표시자인 경우에만 조회/저장을 즉시 실패시키는 전송 함수를 사용합니다. 조회 화면은 빈 상태·기본 문구·명시된 봉사 예시를 보여 줍니다. 저장 성공이나 회원 데이터가 있는 것처럼 꾸미지 않습니다.

실제 로그인·가입 승인·게시물 작성·봉사 인증을 테스트하려면 별도의 개발용 Supabase를 구성하고 마이그레이션/테스트 데이터를 준비하세요. 실제 인증 테스트에서는 `LOCAL_ADMIN_BYPASS=0`으로 바꾼 뒤 서버를 재시작합니다. 실서비스 데이터/키를 사용한 채 관리자 우회 모드를 켜지 마세요. 현재 서버는 이 Mac의 loopback에만 바인딩합니다.

프로덕션에서는 플래그가 1이어도 우회가 작동하지 않습니다. 실제 `next start`로 /home, /admin의 로그인 리다이렉트(307)와 /login 정상 응답을 확인했습니다.

## 바뀐 화면과 자산
- 홈: 압축된 돌고래 히어로 → 공지 → 자주 쓰는 메뉴 → 다음 만남 → 개인 활동 요약.
- 전체 메뉴: 목차형 UI를 하단 시트로 교체. 키보드 포커스 가두기·ESC 닫기·원래 버튼으로 포커스 복귀.
- 하단 메뉴: 홈 / 활동 / 봉사모집 / 커뮤니티 / MY. 생성형 3D 아이콘, 선택 상태, 누름 반응, 안전 영역 여백.
- 로그인: 새 돌고래 이미지, 한국어 인사말, 모바일 입력/버튼. 기존 로그인/승인 처리 유지.
- 커뮤니티: 공지·앨범·익명 탭, 방향키 선택, 새 헤더·작성 폼.
- 활동·봉사·인증·캘린더·쪽지·MY·계정·조·활동 기록: 공통 컴포넌트 적용, 빈 상태와 터치 영역 정비.
- 관리자: 모바일 가로 스크롤 메뉴, 카드/패널 넘침 수정. 데스크톱 관리 기능 유지.
- 로고, 웹 아이콘, Apple 아이콘, PWA 아이콘 교체.
- 3가지 화면 시안 + 실제 사용할 돌고래/로고/9종 아이콘 아틀라스를 이미지 생성으로 제작. 앱용 WebP 3개 합계 약 314KB.
- 3D는 가벼운 생성 이미지와 CSS 움직임입니다. 무거운 WebGL 장면을 추가하지 않았고, 동작 줄이기 설정을 존중합니다.

## Next.js 구조 분석
| 층 | 현재 역할 / 판단 |
|---|---|
| Next.js 16.3.5 · React 19.2.8 | App Router 기반. 초기 디자인 작업 후 별도 보안 패치 업데이트 적용 |
| app/(app) | 승인된 회원 화면, 서버 레이아웃에서 프로필 조회 후 공통 헤더·하단 내비 제공 |
| app/admin | 운영진/관리자 화면과 역할 검사. 회원 앱과 다른 밀도의 관리 레이아웃 유지 |
| middleware.ts | 세션 갱신, 비로그인 리다이렉트. 실제 회원 승인 상태는 getCurrentMember에서 추가 확인 |
| lib/get-current-member.ts | React cache로 요청 내 프로필 조회 중복 방지. 사진은 기존 서명 URL 사용 |
| lib/* 쿼리 | Supabase 서버 조회, 홈 독립 조회는 Promise.all. 화면 리디자인 때문에 DB 모델을 변경하지 않음 |
| Client Components | 로그인 폼·대화상자·필터·탭 등 인터랙션에 사용. 서버 자격 증명을 클라이언트에 추가하지 않음 |
| CSS Modules / globals.css | 기존 스타일 구조 유지, 공통 색·타입·입력·버튼 토큰과 화면별 레이아웃 교체 |
| public/brand + App Router metadata | 새 생성 자산, next/image 최적화, 웹/PWA 아이콘 |
| apps/recruit | 별도 모집 앱. 이번 작업 범위 제외 |

기존 경고: Next의 middleware 파일명은 proxy로 이관을 권장하는 상태입니다. 의존성 설치 시 보안 감사에서 3건(높음 2, 치명적 1)이 보고되었습니다. 이번 디자인 작업에서 자동 강제 업그레이드하지 않았으며, 배포 전 별도 점검이 필요합니다. 전 코드의 보안 감사를 완료했다는 의미는 아닙니다.

## 검증
- ESLint, TypeScript, 프로덕션 빌드 통과.
- 회원/작성/관리자 등 25개 진입 화면 브라우저 확인.
- 홈·로그인·커뮤니티·관리자를 320px, 390px, 844px 가로 화면, 1440px 폭에서 점검.
- 최초 관리자 가로 넘침을 수정하고 재확인.
- 로그인 빈 입력 검증, 전체 메뉴 ESC/포커스 복귀, 커뮤니티 방향키 탭 이동 확인.
- 자료가 필요한 상세/수정 화면, 실제 저장·업로드·가입 승인·푸시 수신은 개발용 DB 연결 후 추가 E2E 필요.
- 근거: `output/design/qa/qa-results.json` 및 같은 폴더의 스크린샷.

## 레퍼런스의 범위
142개 이미지 항목: 모바일 화면 48, 콘셉트 57, 데스크톱 14, 브랜드/모션 23. 모바일·콘셉트 합계 105개이며, 동일한 앱의 공식 실화면 100장을 확보했다는 뜻이 아닙니다. 오래된 자료와 비공식 재디자인도 구분했습니다. UIbowl 유료 자료는 접근하지 않았습니다.

원본 링크와 분류는 `output/design/REFERENCES.md`, 기계 판독용 목록은 `references.json`에 있습니다. 공개 레퍼런스 이미지를 제품 자산으로 복사하지 않았습니다.

## 적용한 디자인 스킬
최종 디자인 검토는 독립 리뷰어가 PASS로 판정했습니다. 문서화 보조 작업에서는 산출물을 받지 못해 중단했고, 주 작업자가 내장 문서화 절차에 따라 DESIGN.md와 .impeccable/design.json을 직접 작성했습니다.

UI UX Pro Max의 모바일 터치/대비 기준, Taste의 명확한 시각 위계, Impeccable의 생성 시안·독립 리뷰·문서화, Open Design의 이미지 중심 편집 구성을 이 앱에 맞게 적용했습니다. 웹툰 콘텐츠의 인물/작품/브랜드를 복제하지 않고, 파란색과 돌고래 정체성을 중심에 두었습니다.
