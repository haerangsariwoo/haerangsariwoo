---
name: Haerangsariwoo Blue Tide
description: 파란 돌고래와 가벼운 입체감이 있는 한국어 봉사동아리 회원 앱
colors:
  primary: "#176ff2"
  primary-hover: "#196eee"
  ink: "#132b50"
  muted-ink: "#66758b"
  canvas: "#f8fbff"
  white: "#ffffff"
  border: "rgba(23, 111, 242, 0.14)"
typography:
  headline:
    fontFamily: "Pretendard, sans-serif"
    fontSize: "28px"
    fontWeight: 700
    letterSpacing: "-0.035em"
  body:
    fontFamily: "Pretendard, sans-serif"
    fontSize: "15px"
    lineHeight: 1.6
  input:
    fontFamily: "Pretendard, sans-serif"
    fontSize: "16px"
  nav-label:
    fontFamily: "Pretendard, sans-serif"
    fontSize: "12px"
    fontWeight: 600
rounded:
  field: "18px"
  button-large: "20px"
  sheet: "26px"
  dock: "27px"
  chip: "15px"
spacing:
  compact: "8px"
  gutter: "16px"
  section: "20px"
components:
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.white}"
    rounded: "{rounded.button-large}"
    height: "56px"
    padding: "0 24px"
  button-outline:
    backgroundColor: "{colors.white}"
    textColor: "{colors.ink}"
    height: "46px"
    padding: "0 18px"
  text-field:
    backgroundColor: "{colors.white}"
    textColor: "{colors.ink}"
    rounded: "{rounded.field}"
    height: "56px"
    padding: "0 16px"
  filter-chip:
    rounded: "{rounded.chip}"
    height: "44px"
    padding: "0 14px"
  content-sheet:
    backgroundColor: "{colors.white}"
    rounded: "{rounded.sheet}"
    padding: "20px"
  bottom-navigation:
    rounded: "{rounded.dock}"
    width: "calc(100% - 24px)"
    padding: "7px 5px 8px"
---

# Design System: 해랑사리우

## Overview

**Creative North Star: "Blue Tide — 작은 실천의 물결"**

Blue Tide는 파란 돌고래를 중심으로 한 해랑사리우 회원 앱의 시각 체계다. 밝은 화면 위에서 한국어 제목과 실제 활동 정보를 먼저 읽고, 생성형 3D 자산은 브랜드 인지와 메뉴 구분을 돕는다. 귀여움은 유지하되 운영 업무의 밀도와 입력 가독성을 우선한다.

**Key Characteristics:**

- 아이스 톤 바탕, 코발트 강조색, 네이비 한국어 타이포그래피.
- 생성한 유광 돌고래·3D 아이콘과 부드러운 곡면.
- 회원 화면은 한 손 탐색, 관리자 화면은 정보 밀도에 맞춘 별도 반응형 구조.

대상은 `apps/main`이다. 모집 앱 `apps/recruit`의 시각 규칙을 바꾸지 않는다.

## Colors

### Primary

Primary는 주요 행동과 선택 상태에 쓰는 선명한 파랑이다. Primary-hover는 기존 버튼의 호버 토큰이다. 홈과 로그인에는 파란 계열의 부드러운 그라데이션과 생성 이미지를 함께 사용한다.

### Neutral

Ink는 제목과 본문, muted-ink는 보조 설명이다. Canvas와 white로 앱 배경과 콘텐츠 영역을 구분하고 border로 얇은 경계를 만든다. 성공·경고·위험은 기존 상태 색 토큰을 유지한다.

**The Identity Rule.** 브랜드 표현은 파란색과 돌고래로 이어가고, 다른 서비스의 상징색을 정체성으로 가져오지 않는다.

## Typography

제목·본문은 앱에 포함된 Pretendard를 사용한다. 루트에 남아 있는 Montserrat 로딩을 새 한국어 제목 체계로 해석하지 않는다.

- 공통 PageHeader는 headline 역할을 따른다.
- 홈의 날씨 카드 제목은 19px이며 현재 기온과 지역을 함께 보여준다. 로그인 제목은 화면별 크기를 사용한다.
- 일반 본문은 body, 입력은 input, 하단 내비 라벨은 nav-label 역할을 따른다.
- 기능성 글자는 실제 HTML로 둔다. 이미지는 글자 없이 브랜드와 입체감만 전달한다.

**The Live Type Rule.** 제목과 동작 이름을 이미지 자산에 넣지 않는다.

## Layout

회원 앱은 최대 480px로 가운데 정렬한다. 넓은 화면에서도 모바일 앱의 읽기 폭을 유지한다. 관리자 레이아웃은 별개로 900px 이하에서 사이드바 대신 가로 스크롤 메뉴를 사용한다.

Sheet의 기본 좌우 여백은 gutter이며, 359px 이하에서는 12px로 줄어든다. 공통 콘텐츠와 홈은 서로 다른 여백 구조를 갖는다. 모든 화면을 같은 카드 그리드로 강제하지 않는다.

고정 하단 내비는 최대 456px, 좌우 12px 여백을 사용한다. 콘텐츠 끝에는 104px와 기기 safe-area를 합산한 공간을 남긴다. 전역 overflow 숨김으로 넘침 문제를 덮지 않는다.

## Elevation & Depth

이미지의 부피감과 밝은 면의 층으로 깊이를 만든다. 회원 앱의 고정 내비에는 흐린 파란 그림자와 반투명 배경을 사용한다. 별도의 WebGL 장면 없이 WebP와 CSS로 재질과 반응을 전달한다.

홈의 장식 이미지와 휴식 타이머는 제거하고 실제 날씨를 표시한다. 메뉴 아이콘은 누를 때 움직이고, 사용자가 요청한 귀여운 3D 반응을 위한 탄성 곡선은 유지한다. 전역 reduced-motion 조건에서는 애니메이션과 전환 시간을 줄인다. 로딩에는 3D 돌고래 대신 작은 선형 파도 표시를 쓴다.

## Shapes

입력은 field, 콘텐츠 그룹은 sheet, 하단 내비는 dock 반경을 따른다. 버튼은 크기별로 반경이 다르다: 작은 버튼 14px, 기본 버튼 16px, 큰 버튼은 button-large다. 기존 관리자 카드·배지에는 별도의 반경이 남아 있다.

이 체계는 부드러운 모서리의 계열이지, 모든 컴포넌트에 하나의 반경을 강제하는 규칙이 아니다.

## Components

### Admin workspace

2026-09-14 관리자 가독성 정리. 기존 248px 사이드바, 76px 상단 바, 9개 메뉴 순서, 페이지별 표와 대시보드 그리드는 유지한다. 메뉴와 대시보드 요약·빠른 작업에는 장식 아이콘을 두지 않으며 실제 기능인 테마 전환·알림 버튼은 유지한다. 작은 기존 앱 로고는 브랜드 표시로 남긴다.

관리자 토큰은 `admin-tokens.css`와 `theme.css`의 `.adminScope`에만 적용한다. 본문은 14px, 표 머리말·보조 문구는 주로 13px, 사이드바 메뉴는 15px이다. 숫자는 고정 폭 숫자를 쓰고 활성 메뉴는 배경과 굵기로 구분한다. 카드에는 그림자 대신 경계선을 쓰며 입력·버튼은 44px을 기본으로 한다. 모바일에서는 기존 가로 스크롤 메뉴와 표 구조를 유지하고 검색 입력은 16px이다. 로그인 생성 시안은 아직 승인 전이므로 아래 Login and splash 규칙을 대체하지 않는다.

### Buttons

Primary는 주요 제출 동작, outline은 보조 동작이다. 작은 버튼은 44px, 기본은 46px, 큰 버튼은 56px 높이다. 눌림은 작은 이동과 축소로 표현하며 disabled 상태에서는 상호작용을 막고 투명도를 낮춘다.

### Inputs / Fields

텍스트 필드는 56px 높이와 16px 입력 글자, 한 줄의 라벨/도움말을 사용한다. 포커스 시 파란 경계를 주고 오류를 메시지와 함께 표시한다. 비밀번호 보기 버튼은 44px 정사각형이다. Placeholder는 muted-ink에 추가 투명도를 적용하지 않는다.

### Chips

기존 FilterChips는 최소 44px 높이로 선택 가능한 필터를 제공한다. 선택 상태를 색뿐 아니라 글자 굵기와 접근성 속성으로도 구분한다. 커뮤니티는 별도의 tablist와 방향키 이동을 갖는다.

### Cards / Containers

흰 콘텐츠 그룹, 얇은 경계, 곡면을 기본으로 한다. 목록형 정보는 기존 행·구분선 구조를 유지한다. 데이터가 없으면 빈 상태를 분명하게 표시한다.

2026-09-14 활동 화면 보완: 큰 소개 상자 대신 유형 필터와 월 달력을 바로 보여준다. 날짜를 선택하면 해당 날짜의 활동만 표시하고, 다시 누르거나 ‘전체 일정’으로 해제한다. 활동 카드는 늘어나지 않는 날짜 칸과 제목, 시간, 장소, 참석 상태로 묶는다. 지난 활동은 접어 두며 봉사 통합 캘린더 링크와 기존 상세·참석 응답 흐름은 유지한다. 연도가 없는 기존 활동은 오늘 기준으로 한 번 해석하며, 달력을 다른 해로 넘겼다고 같은 활동을 반복 생성하지 않는다. 기간 활동은 시작일에 표시하고 카드에 전체 기간을 함께 보여준다.

홈 앨범은 실제 게시글 최대 네 개의 썸네일만 보여준다. 각 사진의 부모가 `position: relative`, 정사각 비율, `overflow: hidden`을 직접 가져야 하며 확대·초점 설정도 그 경계 안에서만 적용한다. 사진 없음과 게시글 없음은 별도로 처리한다. 회귀 검증에 실제 컴포넌트용 예시 데이터와 CSS 클래스 누락 검사를 포함한다.

### Navigation

하단 순서는 활동·봉사모집·홈·커뮤니티·MY다. 가운데 홈만 66px 원형 버튼으로 크게 올리고 MY에는 별도로 생성한 파도 아이콘을 쓴다. 생성한 아이콘에는 실제 텍스트 라벨을 함께 둔다. 헤더의 전체 메뉴는 Radix Dialog 하단 시트이며 닫기·ESC·포커스 복귀를 제공한다. 관리자 진입은 역할에 따라 노출한다.

### Generated Brand Assets

새 앱 아이콘은 사용자가 제공한 하트를 안은 돌고래를 바탕으로 생성한 `public/brand/app-icon-v2.webp`이며 파비콘·PWA·Apple 아이콘에도 적용한다. MY에는 `wave-icon.webp`를 사용한다. 큰 `dolphin-hero.webp`는 로그인에서 제거했다. 이 파일과 `coast-cover.webp`는 이전 시안으로 보관한다. `icon-atlas.webp`는 집·달력·하트 / 대화·돌고래·인증 / 카메라·팀·알림의 3×3 배열이며 BrandIcon이 표시 위치와 가장자리 클리핑을 관리한다. 로고는 실제 워드마크만 표시하며 아래 슬로건은 제거했다.

### Login and splash

로그인은 작은 앱 아이콘과 워드마크를 유지하고, 큰 마스코트 대신 앱 내부의 10초 무음 바다 영상과 실제 HTML 인사말을 사용한다. 영상 출처와 가공 내역은 `output/design-v4/ASSETS.md`에 기록한다. 폼은 밝거나 어두운 단일 곡면으로 영상 아래에 연결하며 학번, 비밀번호, 로그인, 회원가입 순서를 유지한다. 비밀번호 표시 버튼은 키보드 접근이 가능하고 오류 메시지는 해당 입력과 연결한다.

영상은 입력 중, 화면 밖, 다른 탭, 동작 줄이기 설정에서 자동 재생하지 않는다. 지원되는 브라우저의 데이터 절약/2G 연결도 자동 재생을 피한다. 직접 재생·일시정지할 수 있으며, 영상 오류나 자동 재생 제한 시 정지 이미지를 유지한다.

스플래시는 파도 선형 마크와 워드마크만 사용한다. 새 탭 첫 실행에 약 1초 보이고 일반 새로고침이나 앱 내부 이동에서는 반복하지 않는다. 누르기·키보드·포커스로 즉시 종료하며 콘텐츠나 인증, 영상 로딩을 기다리게 하지 않는다. 동작 줄이기 설정과 관리자 화면에서는 생략한다. `?splash=1`로 재확인할 수 있다. 웹앱 시작 화면이며 OS가 표시하는 네이티브 실행 화면을 대체하는 것은 아니다.

2026-09-14 iPhone 실행 보완(위 스플래시 규칙 대체): 현재 돌고래 자산으로 라이트·다크, 세로·가로용 iOS 정적 시작 이미지 52개를 제공한다. `scripts/build-startup-images.mjs`가 `src/lib/startup-images.ts`의 화면 규격을 읽어 생성하며 원본 그림은 바꾸지 않는다. iOS용 Apple 시작 이미지 메타데이터와 manifest 배경을 연결하고, 웹 스플래시를 데이터 의존 콘텐츠 앞에서 스트리밍한다. 초기 배경 CSS는 head에 인라인으로 제공한다.

웹 스플래시는 이미지 로딩·디코딩 후 화면이 보이는 상태에서 두 프레임을 기다리고 1.2초 유지한 뒤 0.22초에 걸쳐 사라진다. 자동 포커스는 종료 조건이 아니며 실제 누르기·키보드로 건너뛸 수 있다. 백그라운드에서는 시간을 소비하지 않고, 이미지 오류·느린 요청·하이드레이션 실패에는 제한 시간 뒤 콘텐츠를 드러낸다. 일반 새로고침·내부 이동에서는 반복하지 않지만 설치 앱의 새 실행은 별도로 감지한다. 인증과 서비스워커의 개인 데이터 캐싱 정책은 변경하지 않는다. iOS 네이티브 시작 단계는 실기기에서 최종 확인해야 하며, 기존 설치는 새 메타데이터를 읽은 뒤 다음 실행부터 새 시작 이미지를 사용할 수 있다.

### Theme and first-use guide

`theme.css`의 의미별 색상 토큰으로 라이트·다크를 구분한다. MY와 전체 메뉴에서 라이트·다크·기기 설정을 선택하며 선택은 브라우저에 저장한다. 로그인과 관리자 헤더에는 빠른 전환 버튼을 둔다. 브랜드 버튼의 흰 글자와 콘텐츠 배경은 별도 토큰이다.

처음 방문한 브라우저에는 회원 앱 진입 시 사용 가이드 → 홈 화면 추가 → 공지 알림 순서의 선택형 설정을 보여준다. 한 번에 한 단계만 열고, 완료하거나 닫은 뒤에는 반복하지 않는다. 기존 가이드를 이미 본 브라우저에는 강제로 다시 띄우지 않는다. MY의 ‘처음 설정 다시 하기’로 전체 과정을 재시작할 수 있다. 설치 상태가 확인되거나 이미 알림을 구독 중이면 해당 설정을 건너뛴다. 설치·알림 권한은 명시적으로 버튼을 눌렀을 때만 요청하며 미지원·차단·서버 설정 누락을 별도로 안내한다.

선택형 7단계 가이드는 실제 활동 메뉴 → 캘린더 → 커뮤니티 → 앨범 탭 → MY → 다크 설정 → 가운데 홈을 누르면 진행한다. 그림 대신 실제 버튼의 위치를 강조하고, 이전·그만 보기·ESC를 제공한다. 첫 설정 중 가이드를 마치거나 그만 보면 설치 안내로 이어지며, 중간 새로고침에도 현재 단계를 이어간다. MY/전체 메뉴의 기존 사용 가이드 재시작은 가이드만 연다. 설정 창은 모바일 하단 시트, 넓은 화면 중앙 다이얼로그이며 제목 초점·포커스 가두기·닫은 뒤 호출 버튼으로 복귀를 제공한다.

### Weather and volunteer information

2026-09-14 문구 정리: 날씨 카드 하단의 위치 안내 펼침 문구와 출처 텍스트 두 줄을 제거했다. 데이터 제공자 링크는 새로고침 옆 44px 정보 아이콘으로 유지하며, 접근성 이름과 툴팁에 Open-Meteo / GeoNames를 명시한다. 날씨 조회·위치 선택 동작은 그대로다.

2026-09-13 추가 요청에 따라 휴식 타이머를 Open-Meteo 현재 날씨와 6시간 예보로 교체했다. 기본은 서울이며 내 위치는 사용자 클릭과 권한 허용 후에만 사용한다. 소수점 두 자리 좌표를 날씨 제공자에게 전달하고 앱에서는 저장하지 않는다. 봉사 지역은 도시 검색으로 선택하고 도시 단위라는 한계를 표시한다. 날씨 시간, 모델 기반 데이터, 출처, 오류와 재시도를 명시한다.

모집 글은 원래 제목을 보존하고 활동 날짜, 시간, 장소, 마감 상태가 먼저 읽히도록 한다. 포털 로고는 24px로 줄이고 제목은 18px로 확대했다. 검색, 모집 중 필터, 출처 탭, 펼쳐 보는 상세 필터를 제공한다. 예시 모집은 실제 모집이 아니라고 표시한다. 고정 UI의 가운데점은 슬래시 또는 문장으로 대체하고 로고 밑 슬로건과 홈의 작은 실천 문구는 제거했다.

## Do's and Don'ts

- Do 파란색과 돌고래 정체성을 유지하고 새 이미지는 기존 생성 자산과 재질·빛 방향을 맞춘다.
- Do 한국어 제목, 실제 데이터, 빈 상태와 오류 상태를 우선한다.
- Do 버튼의 포커스와 선택 상태를 보존하고 동작 줄이기 설정을 존중한다.

- Don't 레퍼런스의 웹툰 작품이나 카카오 브랜드 자산을 제품에 복사한다.
- Don't 이미지 안에 기능성 글자나 버튼을 구워 넣는다.
- Don't 실데이터가 없을 때 실제 참여·승인·저장이 이루어진 것처럼 표시한다.

일부 기존 화면의 작은 장식 캡션과 단단한 오프셋 그림자는 새 화면이 따라야 할 재사용 규칙으로 추출하지 않았다.
